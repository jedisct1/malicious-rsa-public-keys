# Malicious RSA public keys

RSA public keys designed to consume a lot of resources when
doing signature verification or encryption, especially in
applications using OpenSSL.

Includes a valid signed message.
